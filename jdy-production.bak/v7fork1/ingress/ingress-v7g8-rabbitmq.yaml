apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
    ingress.kubernetes.io/custom-response-headers: Env:V7G8
  name: ingress-fork1-rabbitmq
  namespace: ns-fork1
spec:
  tls:
  - hosts:
    - rmq-fork1.jdy.com
    secretName: tls-secret
  rules:
  - host: rmq-fork1.jdy.com
    http:
      paths:
      - path: /
        backend:
          serviceName: rmq-7g8
          servicePort: 15672
